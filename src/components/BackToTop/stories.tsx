import { Meta, Story } from '@storybook/react/types-6-0'
import BackToTop from '.'

export default {
  title: 'BackToTop',
  component: BackToTop
} as Meta

export const Basic: Story = () => <BackToTop />
